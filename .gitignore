infracost-parser-*
infracost-provider-*

# Internal tooling state — never publish.
.synapse/
.worktrees/
CLAUDE.local.md

# `go build ./...` run without -o from a module root drops a binary named
# after the directory; ignore those default names too.
/parser/example/example
/parser/template/template
/provider/example/example
